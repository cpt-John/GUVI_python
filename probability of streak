import fractions


def summation_prob(n):
    if n < 1:
        return "0"
    result = (fractions.Fraction(1, 6**n))
    return f"{result.numerator} {result.denominator}"


print(summation_prob(int(input())))
