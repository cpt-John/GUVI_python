import fractions


def find_coPrime(n, m):
    result = fractions.Fraction(n, m)
    if(result.numerator == n):
        return 1
    return 0


input__ = [int(i) for i in input().split()]
print(find_coPrime(*input__))
