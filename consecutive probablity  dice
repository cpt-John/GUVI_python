import fractions


def summation_prob(n):
    summation = fractions.Fraction(0)
    if n < 1:
        return "0 0"
    summation = (fractions.Fraction(1, 6**n))
    return f"{summation.numerator} {summation.denominator}"


print(summation_prob(int(input())))
